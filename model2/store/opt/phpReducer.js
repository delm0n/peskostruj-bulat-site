const defaultState = {
    сurrentDate: rusData,
    year: dataYear,
  };
  
  
  export const phpReducer = (state = defaultState, action) => {
    switch (action.type) {
      default:
        return state; 
    }
  };
  